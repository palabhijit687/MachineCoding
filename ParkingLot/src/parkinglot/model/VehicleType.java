package parkinglot.model;

/**
 * Types of vehicles we support. Kept small on purpose - in the interview I would
 * ask "how many vehicle types?" and start with these three.
 */
public enum VehicleType {
    BIKE,
    CAR,
    TRUCK
}
